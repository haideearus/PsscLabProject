@model IEnumerable<WebUI.Models.ProductViewModel>
@{
    ViewData["Title"] = "Produse";
}

<h2>Produse</h2>

<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>Nume</th>
        <th>Preț</th>
        <th>Acțiuni</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var product in Model)
    {
        <tr>
            <td>@product.Id</td>
            <td>@product.Name</td>
            <td>@product.Price</td>
            <td>
                <form asp-action="AddToCart" method="post">
                    <input type="hidden" name="productId" value="@product.Id" />
                    <button type="submit" class="btn btn-primary">Adaugă în coș</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>